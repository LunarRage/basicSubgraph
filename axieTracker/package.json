{
  "name": "axieTracker",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create": "graph create axieTracker --node https://admin.dnaoversight.gg/",
    "deploy": "graph deploy  --version-label 0.0.1 --node https://admin.dnaoversight.gg/ axieTracker",
    "create-local": "graph create --node http://localhost:8020/ axieTracker",
    "remove-local": "graph remove --node http://localhost:8020/ axieTracker",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 axieTracker",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.7",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
